{
  "name": "nimble-party",
  "version": "1.2.1",
  "description": "Merge tokens into a shared group token for the Nimble system in Foundry VTT.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
